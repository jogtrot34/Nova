# ──────────────────────────────────────────────────────────────────────
# Nova — full project requirements
# Python 3.10 – 3.12 all work fine (tested dependency resolution on 3.12)
#
# IMPORTANT: install CPU-only PyTorch FIRST, as its own command, before
# this file. See "How to run it" below for why — a plain entry in this
# file is not reliable enough to guarantee you skip the NVIDIA packages.
# ──────────────────────────────────────────────────────────────────────

# ── Face recognition ─────────────────────────────────────────────────
opencv-python>=4.9.0
numpy>=1.24
face_recognition>=1.3.0
dlib>=19.24.6          # ships prebuilt wheels for most Python versions;
                        # on very new interpreters (3.13/3.14) it may
                        # build from source — see HOW_TO_RUN.md for the
                        # compiler packages that needs

# ── Voice recognition (resemblyzer's own deps, minus torch — see above) ─
resemblyzer>=0.1.4
sounddevice>=0.4.6
webrtcvad>=2.0.10
librosa>=0.10.0
scipy>=1.10.0

# ── GSM modem — calls, SMS, USSD (console.py / ussd.py / notifier.py) ─
pyserial>=3.5

# ── Web backend + UI (FastAPI, replaces the old Flet desktop UI) ──────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9

# ── OPTIONAL: fast_responder.py (the standalone chatbot scaffold) ─────
# Only needed if you actually start using fast_responder.py /
# piper_speak.py. Not required for anything else — leave commented
# out until you decide to wire it in.
# nltk>=3.8
# SpeechRecognition>=3.10
# piper-tts>=1.2.0
